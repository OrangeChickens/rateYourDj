/* pages/my-invites/my-invites.wxss - NTS Style */

.invites-container {
  min-height: 100vh;
  background: #FFFFFF;
  padding: 32rpx;
  box-sizing: border-box;
}

/* 头部 */
.invites-header {
  background: #F5F5F5;
  border: 3rpx solid #000;
  padding: 40rpx 32rpx;
  margin-bottom: 48rpx;
}

.quota-card {
  text-align: center;
  margin-bottom: 32rpx;
}

.quota-label {
  display: block;
  font-size: 20rpx;
  font-weight: 700;
  color: #999;
  text-transform: uppercase;
  letter-spacing: 2rpx;
  margin-bottom: 16rpx;
}

.quota-number {
  display: block;
  font-size: 120rpx;
  font-weight: 900;
  color: #000;
  line-height: 1;
  letter-spacing: -3rpx;
  margin-bottom: 8rpx;
}

.quota-hint {
  display: block;
  font-size: 24rpx;
  color: #666;
  font-weight: 500;
}

.generate-btn {
  width: 100%;
  height: 88rpx;
  background: #000;
  color: #FFF;
  font-size: 28rpx;
  font-weight: 700;
  border: none;
  border-radius: 0;
  text-transform: uppercase;
  letter-spacing: 2rpx;
  margin-bottom: 24rpx;
}

.generate-btn::after {
  border: none;
}

.generate-btn[disabled] {
  background: #E0E0E0;
  color: #999;
}

.generate-btn:active:not([disabled]) {
  opacity: 0.8;
}

.quota-notice {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16rpx;
  padding: 32rpx;
  background: #FFF;
  border: 3rpx solid #000;
}

.notice-text {
  font-size: 24rpx;
  color: #666;
  font-weight: 500;
  text-align: center;
  line-height: 1.5;
}

.go-tasks-btn {
  padding: 16rpx 32rpx;
  background: #000;
  color: #FFF;
  font-size: 22rpx;
  font-weight: 700;
  border: none;
  border-radius: 0;
  text-transform: uppercase;
  letter-spacing: 2rpx;
}

.go-tasks-btn::after {
  border: none;
}

.go-tasks-btn:active {
  opacity: 0.8;
}

/* 邀请码列表 */
.codes-section {
  margin-bottom: 40rpx;
}

.section-title {
  font-size: 32rpx;
  font-weight: 900;
  color: #000;
  text-transform: uppercase;
  letter-spacing: 2rpx;
  margin-bottom: 32rpx;
  padding-bottom: 16rpx;
  border-bottom: 3rpx solid #000;
}

/* 空状态 */
.empty-state {
  text-align: center;
  padding: 120rpx 48rpx;
  background: #F5F5F5;
  border: 3rpx solid #000;
}

.empty-text {
  display: block;
  font-size: 28rpx;
  font-weight: 700;
  color: #000;
  margin-bottom: 12rpx;
  text-transform: uppercase;
  letter-spacing: 1rpx;
}

.empty-hint {
  display: block;
  font-size: 24rpx;
  color: #666;
}

/* 邀请码卡片 */
.code-card {
  background: #F5F5F5;
  border: 3rpx solid #000;
  padding: 32rpx;
  margin-bottom: 24rpx;
  box-sizing: border-box;
}

.code-card.used {
  opacity: 0.5;
  border-color: #999;
}

.code-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 24rpx;
  padding-bottom: 24rpx;
  border-bottom: 2rpx solid #000;
}

.code-text {
  font-size: 36rpx;
  font-weight: 900;
  color: #000;
  font-family: 'Courier New', monospace;
  letter-spacing: 3rpx;
}

.code-status {
  padding: 10rpx 20rpx;
  background: #FFF;
  border: 3rpx solid #000;
  font-size: 20rpx;
  font-weight: 900;
  text-transform: uppercase;
  letter-spacing: 1rpx;
}

.code-status.used {
  background: #000;
  color: #FFF;
}

.code-info {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
  margin-bottom: 24rpx;
}

.info-item {
  font-size: 24rpx;
  color: #666;
  font-weight: 500;
}

/* 使用记录 */
.used-by-section {
  background: #FFF;
  padding: 24rpx;
  margin-bottom: 24rpx;
  border: 2rpx solid #000;
}

.used-by-title {
  display: block;
  font-size: 20rpx;
  font-weight: 700;
  color: #999;
  text-transform: uppercase;
  letter-spacing: 2rpx;
  margin-bottom: 16rpx;
}

.used-by-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16rpx 0;
  border-bottom: 2rpx solid #E0E0E0;
}

.used-by-item:last-child {
  border-bottom: none;
}

.user-nickname {
  font-size: 26rpx;
  font-weight: 700;
  color: #000;
}

.user-time {
  font-size: 22rpx;
  color: #999;
  font-weight: 500;
}

/* 操作按钮 */
.code-actions {
  display: flex;
  gap: 16rpx;
}

.action-btn {
  flex: 1;
  height: 80rpx;
  font-size: 26rpx;
  font-weight: 900;
  border: none;
  border-radius: 0;
  text-transform: uppercase;
  letter-spacing: 2rpx;
  border: 3rpx solid #000;
  box-sizing: border-box;
}

.action-btn::after {
  border: none;
}

.action-btn.copy {
  background: #FFF;
  color: #000;
}

.action-btn.share {
  background: #000;
  color: #FFF;
}

.action-btn:active {
  opacity: 0.8;
}
